# Backend API (Spring Boot)
FINANCE_API_BASE_URL=http://localhost:8080

# LLM - never commit real keys; use .env (gitignored)
OPENAI_API_KEY=

# Optional: restrict which model is used
OPENAI_MODEL=gpt-4o-mini
